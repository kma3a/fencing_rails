<%= form_for @student do |f| %>
<% if @student.errors %>
  <% @student.errors.full_messages.each do |message| %>
    <p><%=message%></p>
  <%end%>
<%end%>
  <%= f.text_field :name, placeholder:"Student's Name", class:"form-control" %></br>
  <%= f.submit "Create Student", class:"btn"%>
<%end%>
