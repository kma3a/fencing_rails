<h1 class="intro"><%= @team.name %> </h1>
<h3>Headcoach: <%= @team.headcoach.name %> </h3>
<div class="coach">
<h3>Coaches:</h3>
<% if  current_coach.id == @team.headcoach_id%>
  <%= form_tag(controller: "teams", action: "add_coach", method: "get") do %>
    <%= email_field(:coach, :email, class:"form", placeholder:"Coach's Email") %>
   <%= submit_tag("Add Coach") %>
  <% end %>
<%end%>
</br>
<% @team.coaches.each do |coach| %>
  <li class="list-group-item"><%= coach.name %> <% if current_coach.id == @team.headcoach_id %> <%= link_to "Remove", remove_coach_path({coach_id: coach.id}) %> <%end%></li>
<%end%>
</div>
<div class="col-md-6">
  <h3>Students:</h3>
  <div>
  <% if current_coach.id == @team.headcoach_id %>
      <%= form_tag(controller: "teams", action: "add_student", method: "post") do %>
      <%= text_field(:student, :secret_key, placeholder:"Student's Key") %>
      <%= submit_tag("Add Student") %>
    <% end %>
  <%end%>
  </div> </br>
  <ul class="list-group">
  <% @team.students.each do |student| %>
    <li class="list-group-item">
      <%=link_to student.name, student_path(student.secret_key) %> <% if current_coach.id == @team.headcoach_id %>  <%= link_to "Remove Student", remove_student_path({student_id: student.id})%>  <%= link_to "Edit Student", edit_student_path(student.secret_key) %> 
    <%end%>
    </li>
  <%end%>
  </ul>
</div>

<div class="col-md-6">
  <h3>Events: </h3>
  <% if coach_signed_in? %>
    <%= link_to "Create Event", new_team_event_path(@team,@event) %>
  <% end %>
  </br></br>
 <ul class="list-group">
    <% @team.events.each do |event| %>
      <li class="list-group-item"><%= link_to event.event_title, team_event_path(@team, event)%> <%= link_to "Edit Event", edit_team_event_path(@team, event)%></li>
    <%end%>
  </ul>
</div>

